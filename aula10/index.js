// tipos de dados primitivos: string, number, undefined, null, boolean.

const nome = 'Gabriel'; //String
const nome1 = "Gabriel"; //String
const nome2 = `Gabriel`; //String

const num = 10; // Number
const num2 = 10.10; // Number

let nomeAluno; // undefined = < aviso de que falta definir uma variável. Não aponta pra lugar nenhum na memória.
const sobreNome = null; // < usamos quando queremos, Não aponta para lugar nenhum na memória.

let avaliar = true; // true/false. verdadeiro ou falso. (Lógico)

console.log(typeof avaliar, avaliar); // verificar os tipos das variáveis e o valor dela.

let a = 2;
const b = a;

a = 3;
console.log(a, b); // 3, 2





